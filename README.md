## Compilação do Projeto

No diretório raiz do projeto existe um Makefile que automatiza o processo de compilação. Desta forma, para obter o executável de nome TP1 no diretório raiz do projeto basta realizar a chamada do comando make.
